enum SettingTypeEnum {
    STRING = "string",
    NUMERIC = "numeric",
    BOOLEAN = "boolean",
    DATE = "date",
    TIME = "time",
    DATETIME = "datetime",
    TRANSLATABLE = "translatable",
    TEXT = "text",
    HTML_TEXT = "HTML_TEXT",
    SELECT = "select",
    RESOURCE = "resource",
}

export default SettingTypeEnum;
